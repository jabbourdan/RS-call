# Production API base URL — consumed by scripts/generate-env.js to build environment.prod.ts.
# On Vercel, set this in Project Settings → Environment Variables instead of committing it.
API_URL=https://api.kolligent.co.il/api/v1
